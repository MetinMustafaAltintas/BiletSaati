@model GetPlaceAdminPageVM

@if (TempData["Message"] != null)
{
    <h1 class="alert alert-danger">
        <b>@TempData["Message"]!</b>
    </h1>
}
<!-- page start-->
<div class="row">
    <div class="col-lg-12">
        <section class="panel">
            <header class="panel-heading">
                Mekanlar Tablosu
            </header>
            <div class="panel-body">
                <div class="adv-table">
                    <table class="display table table-bordered table-striped" id="example">
                        <thead>
                            <tr>
                                <th>Mekan ID</th>
                                <th>Mekan Ismi</th>
                                <th>Şehir Ismi</th>
                                <th class="center hidden-phone">Durumu</th>
                                <th class="center hidden-phone">Guncelle</th>
                                <th class="center hidden-phone">Pasife Al</th>
                                <th class="center hidden-phone">Sil</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (GetPlaceAdminPureVM item in Model.GetPlaceAdminPureVMs)
                            {
                                <tr class="gradeX">
                                    <td>@item.ID</td>
                                    <td>@item.PlaceName</td>
                                    <td>@item.CityName</td>
                                    <td>@item.Status</td>
                                    <td class="center hidden-phone"><a asp-action="UpdatePlace" asp-route-id="@item.ID" class="btn btn-warning">Güncelle</a></td>
                                    <td class="center hidden-phone"><a asp-action="DeletePlace" asp-route-id="@item.ID" class="btn btn-info">Pasife Al</a></td>
                                    <td class="center hidden-phone"><a asp-action="DestroyPlace" asp-route-id="@item.ID" class="btn btn-danger">Sil</a></td>
                                </tr>
                            }
                        </tbody>
                        <tfoot>
                            <tr>
                                <th>Mekan ID</th>
                                <th>Mekan Ismi</th>
                                <th>Şehir Ismi</th>
                                <th class="center hidden-phone">Durumu</th>
                                <th class="center hidden-phone">Guncelle</th>
                                <th class="center hidden-phone">Pasife Al</th>
                                <th class="center hidden-phone">Sil</th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        </section>
    </div>
</div>